[project]
name = "cataloguecanvas-website"
version = "0.1.0"
description = "Documentation website for CatalogueCanvas (MkDocs Material)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
]
